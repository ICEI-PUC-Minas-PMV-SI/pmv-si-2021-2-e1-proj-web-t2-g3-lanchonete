const c = (el)=>document.querySelector(el)
const cs = (el)=>document.querySelectorAll(el)

modelsJson.map((item,index)=>{
     let valorprodutoqnt = document.querySelector ('qnt, badição, bsubtração').cloneNode(true)
     document.querySelector('qnt, badição, bsubtração').append(valorprodutoqnt);
});